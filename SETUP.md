# Инструкция по настройке AI Chat

## Шаг 1: Настройка GigaChat API

1. Перейдите на [GigaChat Developer Portal](https://developers.sber.ru/portal/products/gigachat)
2. Зарегистрируйтесь или войдите в аккаунт
3. Создайте новое приложение
4. Получите **API ключ** для вашего приложения

## Шаг 2: Настройка сервера

1. Создайте файл `.env` в папке `server/`:
   ```bash
   cd server
   cp env.example .env
   ```

2. Отредактируйте `.env` файл, заменив `your_gigachat_api_key_here` на ваш API ключ:
   ```env
   GIGACHAT_API_KEY=ваш_api_ключ_здесь
   PORT=3001
   NODE_ENV=development
   ```

## Шаг 3: Запуск приложения

### Вариант 1: Автоматический запуск
```bash
./start.sh
```

### Вариант 2: Ручной запуск

**Терминал 1 - Сервер:**
```bash
cd server
npm start
```

**Терминал 2 - Клиент:**
```bash
cd client
npm start
```

## Шаг 4: Проверка работы

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Вы должны увидеть интерфейс чата
3. Попробуйте открыть настройки (кнопка ⚙️ в заголовке)
4. Отправьте тестовое сообщение
5. AI должен ответить (если API ключи настроены правильно)

## Шаг 5: Настройка чата

### Доступные настройки:
- **Temperature** - Креативность ответов (0.0-2.0)
- **Max Tokens** - Максимальная длина ответа (1-4096)
- **Top P** - Разнообразие ответов (0.0-1.0)
- **Frequency Penalty** - Избегание повторений (-2.0-2.0)
- **Presence Penalty** - Избегание новых тем (-2.0-2.0)
- **System Message** - Поведение AI
- **Safety Settings** - Фильтрация контента

### Управление:
- ⚙️ - Открыть настройки
- 🗑️ - Очистить историю чата
- Настройки сохраняются автоматически

## Возможные проблемы

### ❌ "Ошибка авторизации"
- Проверьте правильность API ключа в `.env`
- Убедитесь, что API ключ активен и не истек

### ❌ "Ошибка подключения к серверу"
- Убедитесь, что сервер запущен на порту 3001
- Проверьте, что нет других процессов на этом порту

### ❌ "CORS ошибка"
- Убедитесь, что клиент запущен на порту 3000
- Проверьте настройки CORS в `server/server.js`

## Готово! 🎉

Теперь у вас есть полнофункциональное AI чат-приложение с красивым интерфейсом и интеграцией с GigaChat API.
